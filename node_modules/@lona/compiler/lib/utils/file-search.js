"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const Glob = __importStar(require("glob"));
/** search for files respecting the glob patter in the workspace.
 * @returns an array of relative paths
 */
exports.sync = (workspacePath, glob, options = {}) => {
    return Glob.sync(glob, Object.assign(Object.assign({}, options), { cwd: workspacePath }));
};
