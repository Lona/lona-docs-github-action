"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../../utils");
exports.findChildPages = (root) => {
    return root.children
        .map(child => {
        if (child.type === 'page') {
            return child.data.url;
        }
    })
        .filter(utils_1.nonNullable);
};
