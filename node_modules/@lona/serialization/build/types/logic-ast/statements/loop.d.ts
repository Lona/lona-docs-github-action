import { Expression, Statement, UUID } from '../';
export declare type LoopStatement = {
    type: 'loop';
    data: {
        expression: Expression;
        block: Statement[];
        id: UUID;
    };
};
