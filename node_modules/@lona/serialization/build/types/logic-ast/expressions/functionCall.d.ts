import { Expression, FunctionCallArgument, UUID } from '../';
export declare type FunctionCallExpression = {
    type: 'functionCallExpression';
    data: {
        id: UUID;
        expression: Expression;
        arguments: FunctionCallArgument[];
    };
};
