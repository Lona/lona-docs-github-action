"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function repeat(s, x) {
    let res = '';
    for (let i = 0; i < x; i++) {
        res += s;
    }
    return res;
}
exports.repeat = repeat;
function indentLine(line, indent) {
    return line ? indent + line : line;
}
exports.indentLine = indentLine;
function indentLines(lines, indent) {
    return lines.map(line => indentLine(line, indent));
}
exports.indentLines = indentLines;
function indentBlock(codeblock, indent) {
    return indentLines(codeblock.split('\n'), repeat(' ', indent)).join('\n');
}
exports.indentBlock = indentBlock;
function indentBlockWithFirstLinePrefix(codeblock, prefix) {
    const lines = codeblock.split('\n');
    return [
        prefix + lines[0],
        ...indentLines(lines.slice(1), repeat(' ', prefix.length)),
    ].join('\n');
}
exports.indentBlockWithFirstLinePrefix = indentBlockWithFirstLinePrefix;
