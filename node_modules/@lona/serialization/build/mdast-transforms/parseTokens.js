"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const unist_util_visit_1 = __importDefault(require("unist-util-visit"));
const lona_format_1 = require("../lona-format");
const lona_logic_1 = require("../lona-logic");
function parseTokens(src, filePath) {
    return (ast) => {
        unist_util_visit_1.default(ast, 'code', (node) => {
            if (node.lang === 'tokens') {
                // get the position of the node
                const pre = src.split(node.value)[0];
                const offset = pre.split('\n').length - 1;
                node.parsed = JSON.parse(lona_logic_1.convertLogic(node.value, lona_format_1.SERIALIZATION_FORMAT.JSON, {
                    filePath,
                    offset,
                }));
            }
            return node;
        });
        return ast;
    };
}
exports.default = parseTokens;
